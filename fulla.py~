# Simple script to use fulla (hugin bundle, barrel distortion correction). 
# I chmod it and put some image from my camera into the same folder. I think it is quite helpful sometime.

#!/usr/bin/python

import os, glob, Image

pic_list = []

pic_list = glob.glob('*.jpg')
pic_list.sort()

print len(pic_list), 'images found...'
for temp in pic_list:
    print temp


cmd = 'fulla -g 0.0216776:-0.0799067:0.0566601:1 '
for temp in pic_list:
    repair = cmd + temp
    os.popen(repair).read()
